const XLSX = require('xlsx');// Municipios estándar de la Appconst POBLACIONES_APP = [    "ARMENIA", "QUIMBAYA", "MONTENEGRO", "CALARCA", "CIRCASIA", "TEBAIDA", "SALENTO", "FILANDIA", "GENOVA",    "MANIZALES - VILLAMARIA", "CHINCHINA", "NEIRA", "RIOSUCIO", "SUPIA", "MARMATO", "PACORA", "AGUADAS", "ANSERMA", "VITERBO",    "PEREIRA", "DOSQUEBRADAS", "SANTA ROSA", "LA VIRGINIA", "MARSELLA", "BELEN", "MISTRATO", "GUATICA", "QUINCHIA", "PUEBLO RICO", "SANTUARIO APIA", "ARGELIA EL CAIRO", "CARTAGO"];const workbook = XLSX.readFile('c:/Users/Juanjo/Downloads/RUTERO ALPINA - FLEISCHAMNN (1).xlsx');const blocks = [    { day: 'Lunes', col: 0, rOff: 0 }, { day: 'Martes', col: 8, rOff: 0 },    { day: 'Miercoles', col: 0, rOff: 18 }, { day: 'Jueves', col: 8, rOff: 18 },    { day: 'Viernes', col: 0, rOff: 36 }, { day: 'Sabado', col: 8, rOff: 36 }];console.log('--- VERIFICACIÓN DE MAPEO FINAL (CALDOS/QUINDIO/RISARALDA) ---');blocks.forEach(block => {    console.log(`\n=== ${block.day.toUpperCase()} ===`);    workbook.SheetNames.forEach(sheetName => {        const sheet = workbook.Sheets[sheetName];        const rows = XLSX.utils.sheet_to_json(sheet, { header: 1 });        const blockRows = rows.slice(block.rOff + 2, block.rOff + 17);        blockRows.forEach(row => {            const alp = String(row[block.col] || '').trim();            const muniRaw = String(row[block.col + 2] || '').trim().toUpperCase();            if (alp && alp !== 'null' && alp.length > 1 && muniRaw) {                // Buscar el match que hace el código                const normalize = (s) => s.replace(/[\s-]/g, '');                const match = POBLACIONES_APP.find(p => {                    const nTitle = normalize(p);                    const nMuni = normalize(muniRaw);                    return nMuni.includes(nTitle) || nTitle.includes(nMuni);                });                if (match) {                    console.log(`[${sheetName}] Zona: ${alp.split(' ')[0]} -> ${match}`);                } else {                    console.log(`[${sheetName}] Zona: ${alp.split(' ')[0]} -> UNMAPPED (${muniRaw})`);                }            }        });    });});